import React from 'react';

export default () => (
  <div>
    Login
    <form>
      <input
        type="text"
        placeholder="Username"
      />
      <input
        type="password"
        placeholder="Password"
      />
    </form>
  </div>
);
